# B2C-LIVE — Documentation

`01-happy-path.md` — Step-by-step walkthrough of a live commerce transaction including OTT-embedded and group-buy variants.

For the machine-readable API sequence, see `../spine.yaml`.
For source attribution fields, see `releases/release1/schema/extension/trade/TradeContract/v1/attributes.yaml#source`.
