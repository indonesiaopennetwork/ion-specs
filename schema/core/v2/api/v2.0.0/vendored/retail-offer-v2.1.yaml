# VENDORED UPSTREAM SCHEMA — RetailOffer v2.1
# Remote URL: https://raw.githubusercontent.com/beckn/local-retail/main/schema/RetailOffer/v2.1/attributes.yaml
# Pinned: 2026-06-02
# Status: STUB — fetch from remote URL and replace this file with actual content
#
# To fetch:
#   curl -o retail-offer-v2.1.yaml "https://raw.githubusercontent.com/beckn/local-retail/main/schema/RetailOffer/v2.1/attributes.yaml"
#
# After fetching, verify schema name RetailOffer is present and update
# docs/ION_Upstream_Dependency_Map.md with the inherited objects list.
#
# This stub allows the repo to be structurally complete and the allOf $refs
# to be declared correctly while the actual fetch is pending.

openapi: 3.1.1
info:
  title: "RetailOffer v2.1 — STUB"
  description: >
    STUB FILE. Replace with actual content fetched from:
    https://raw.githubusercontent.com/beckn/local-retail/main/schema/RetailOffer/v2.1/attributes.yaml
  version: "v2.1"
components:
  schemas:
    RetailOffer:
      type: object
      description: "STUB — see remote URL above for actual schema definition"
      additionalProperties: true
      allOf:
        - $ref: ../beckn.yaml#/components/schemas/Attributes
