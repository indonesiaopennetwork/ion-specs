# VENDORED UPSTREAM SCHEMA — Shipment v2.0
# Remote URL: https://raw.githubusercontent.com/beckn/schemas/main/schema/Shipment/v2.0/attributes.yaml
# Pinned: 2026-06-02
# Status: STUB — fetch from remote URL and replace this file with actual content
#
# To fetch:
#   curl -o shipment-v2.0.yaml "https://raw.githubusercontent.com/beckn/schemas/main/schema/Shipment/v2.0/attributes.yaml"
#
# After fetching, verify schema name Shipment is present and update
# docs/ION_Upstream_Dependency_Map.md with the inherited objects list.
#
# This stub allows the repo to be structurally complete and the allOf $refs
# to be declared correctly while the actual fetch is pending.

openapi: 3.1.1
info:
  title: "Shipment v2.0 — STUB"
  description: >
    STUB FILE. Replace with actual content fetched from:
    https://raw.githubusercontent.com/beckn/schemas/main/schema/Shipment/v2.0/attributes.yaml
  version: "v2.0"
components:
  schemas:
    Shipment:
      type: object
      description: "STUB — see remote URL above for actual schema definition"
      additionalProperties: true
      allOf:
        - $ref: ../beckn.yaml#/components/schemas/Attributes
