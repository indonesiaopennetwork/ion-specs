# RestaurantOffer — Overview

**Pack:** `hospitality/offer/v1`  
**Beckn slot:** `Offer.offerAttributes`  
**CRC:** HSC-restaurant-ordering  

## Purpose

RestaurantOffer provides delivery offer, pricing, scheduling, promotions for online food ordering.

## Attachment point

`Offer.offerAttributes` — see `x-beckn-attaches-to` in `attributes.yaml`.

## Conditional requirements

Fields with `x-ion-condition:` are enforced by ONIX Rego policy.
See `docs/ION_ONIX_Conditional_Policy.md` in the repo root.

## Examples

See `examples/` for complete payloads.
