# RestaurantProvider — Overview

**Pack:** `hospitality/provider/v1`  
**Beckn slot:** `Provider.providerAttributes`  
**CRC:** HSC-restaurant-ordering  

## Purpose

RestaurantProvider provides restaurant and cloud kitchen provider profile for online food ordering.

## Attachment point

`Provider.providerAttributes` — see `x-beckn-attaches-to` in `attributes.yaml`.

## Conditional requirements

Fields with `x-ion-condition:` are enforced by ONIX Rego policy.
See `docs/ION_ONIX_Conditional_Policy.md` in the repo root.

## Examples

See `examples/` for complete payloads.
