# HospitalityConsideration — Overview

**Pack:** `hospitality/consideration/v1`  
**Beckn slot:** `Consideration.considerationAttributes`  
**CRC:** HSC-restaurant-ordering  

## Purpose

HospitalityConsideration provides price breakdown, fees, tax, cashback for online food ordering.

## Attachment point

`Consideration.considerationAttributes` — see `x-beckn-attaches-to` in `attributes.yaml`.

## Conditional requirements

Fields with `x-ion-condition:` are enforced by ONIX Rego policy.
See `docs/ION_ONIX_Conditional_Policy.md` in the repo root.

## Examples

See `examples/` for complete payloads.
