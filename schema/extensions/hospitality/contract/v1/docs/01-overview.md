# HospitalityContract — Overview

**Pack:** `hospitality/contract/v1`  
**Beckn slot:** `Contract.contractAttributes`  
**CRC:** HSC-restaurant-ordering  

## Purpose

HospitalityContract provides confirmed order, delivery prefs, group order, gifting for online food ordering.

## Attachment point

`Contract.contractAttributes` — see `x-beckn-attaches-to` in `attributes.yaml`.

## Conditional requirements

Fields with `x-ion-condition:` are enforced by ONIX Rego policy.
See `docs/ION_ONIX_Conditional_Policy.md` in the repo root.

## Examples

See `examples/` for complete payloads.
