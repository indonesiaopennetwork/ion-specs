{
  "id": "ion-trade-consideration-v1",
  "version": "1.0.0",
  "domain": "trade",
  "name": "ION Trade Consideration Extension (v1)",
  "description": "Retail consideration amounts and breakup with ION tax-detail refinement.",
  "packSchema": "./schema.json",
  "jsonldContext": "./context.jsonld",
  "attachmentPoints": {
    "considerationAttributes": [
      "IONTradeConsideration"
    ]
  },
  "extends": "beckn:Consideration",
  "type": "AttributePack",
  "indexing": {
    "indexable": [
      {
        "path": "$.breakup[].type"
      },
      {
        "path": "$.totalAmount"
      },
      {
        "path": "$.breakup[].taxDetail.taxRegime"
      }
    ],
    "filterable": [
      {
        "path": "$.breakup[].type"
      },
      {
        "path": "$.totalAmount"
      },
      {
        "path": "$.breakup[].taxDetail.taxRegime"
      }
    ],
    "sortable": []
  },
  "minimalForDiscovery": [
    "totalAmount",
    "currency"
  ],
  "x-ion-layer": {
    "generic": "TradeConsideration",
    "ion": "IONTradeConsideration",
    "splitVersion": "0.7.0"
  }
}
